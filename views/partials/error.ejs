<% if (error && error.length > 0) { %>
      <div class="max-w-md mx-auto bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-1" role="alert">
        <strong class="font-bold">Please correct the following errors:</strong>
        <ul class="list-disc list-inside mt-1 text-sm">
          <% error.forEach(err => { %>
            <li><%= err %></li>
          <% }) %>
        </ul>
      </div>
    <% } %>